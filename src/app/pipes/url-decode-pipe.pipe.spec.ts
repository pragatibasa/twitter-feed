import { UrlDecodePipePipe } from './url-decode-pipe.pipe';

describe('UrlDecodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlDecodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
