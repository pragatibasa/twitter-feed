import { TransformTweetTextPipe } from './transform-tweet-text.pipe';

describe('TransformTweetTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTweetTextPipe();
    expect(pipe).toBeTruthy();
  });
});
